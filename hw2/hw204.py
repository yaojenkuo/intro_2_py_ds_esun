# coding=utf-8
'''
實作用二分搜尋法逼進平方根的函數 `my_sqrt()`，逼近區間可以自行決定
Author: XXX
'''

def my_sqrt(x):
    '逼進平方根的函數'
    low = 0.0
    high = float(x)
    diff = __
    while __ - __ > diff:
        mid = (low + high) / 2
        if mid**2 > x:
            __ = __
        else:
            __ = __
    return low